export TERM
while ! tput longname >/dev/null 2>&1; do
	case "$TERM" in
		*-?*color) TERM=${TERM%%-*}-color;
		*-*)       TERM=${TERM%%-*};;
		xterm)     break;;
		*)         TERM=xterm;;
	esac
done

# vim: ft=sh
